---
interface Props {
  service: {
    title: string
    shortDescription?: string
    slug: { current: string } | string
    iconImageUrl?: string
  }
  fallbackIcon?: string | null
  cellClass?: string
}
const { service, fallbackIcon, cellClass = '' } = Astro.props as Props
const slug = typeof service.slug === 'string' ? service.slug : service.slug?.current
---

<a
  href={slug ? `/services/${slug}` : '/services'}
  class={`group flex flex-col justify-center bg-bg p-8 min-h-[320px] transition-colors hover:bg-bg-2 ${cellClass}`}
>
  {service.iconImageUrl ? (
    <img src={service.iconImageUrl} alt="" class="w-10 h-10 shrink-0 object-contain" />
  ) : fallbackIcon ? (
    <div class="w-10 h-10 shrink-0 text-accent transition-colors" set:html={fallbackIcon} />
  ) : null}
  <h3 class="text-text-white group-hover:text-accent-light font-bold text-xl mt-6 mb-3 transition-colors">
    {service.title}
  </h3>
  {service.shortDescription && (
    <p class="text-text-muted text-base leading-6 min-h-[72px] transition-colors">
      {service.shortDescription}
    </p>
  )}
  <span class="mt-4 inline-flex items-center gap-1.5 self-start border-b-2 border-current text-accent-text text-sm font-medium pb-0.5 opacity-0 group-hover:opacity-100 transition-opacity duration-200">
    Learn more
    <svg class="w-3.5 h-3.5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
      <path d="M5 12h14M13 6l6 6-6 6" stroke-linecap="round" stroke-linejoin="round" />
    </svg>
  </span>
</a>
